const rewire = require('rewire');
const { applyFilter } = require('../lib/filters.js');
// const applyFilter = rewire('../lib/filters.js').__get__("applyFilter");

describe('applyFilter', () => {
  beforeEach(() => {
    
  });

  const allIncidents = require('./fixtures/statuspage-all.fixture.json');
  it('', () => {

  });

});
